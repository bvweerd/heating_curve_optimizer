name: Create Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: write

jobs:
  release:
    name: Build ZIP and create GitHub Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Get version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create release ZIP
        run: |
          cd custom_components/heating_curve_optimizer/
          zip ../../heating_curve_optimizer.zip -r ./

      - name: Generate changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@v6
        with:
          configuration: |
            {
              "categories": [
                { "title": "## ✨ New features", "labels": ["enhancement", "feature"] },
                { "title": "## 🐛 Bug fixes", "labels": ["bug", "fix"] },
                { "title": "## 📚 Documentation", "labels": ["documentation"] },
                { "title": "## 🔧 Other", "labels": [] }
              ],
              "template": "#{{CHANGELOG}}\n\n**Full changelog**: #{{RELEASE_DIFF}}"
            }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: heating_curve_optimizer.zip
          body: |
            ## Installation via HACS
            Install or update via HACS — the new version will be offered automatically.

            ## Manual installation
            Download `heating_curve_optimizer.zip`, extract and copy `heating_curve_optimizer/` to your `custom_components/` directory.

            ${{ steps.changelog.outputs.changelog }}
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
